requests>=2.31.0
beautifulsoup4>=4.12.0
curl_cffi>=0.7.0  # Free browser-grade TLS impersonation (hybrid scraping)
bcrypt>=4.0.0     # Password hashing for the Streamlit dashboard auth
openpyxl>=3.1.0   # Read CGPJ Excel reports (macro_data / cgpj_lanzamientos)
streamlit>=1.36.0
pandas>=2.0.0
plotly>=5.18.0
python-dotenv>=1.0.0
gdown>=4.7.1
urllib3>=2.0.0

# Postgres backend (Phase 3 of the data layer migration)
# psycopg 3 — modern driver with native asyncio support and a built-in pool.
# Pin to >=3.1 so we get the maintained branch. The [binary] extra ships a
# wheel with libpq embedded, so users don't need a system Postgres install.
psycopg[binary]>=3.1.0
psycopg_pool>=3.2.0
alembic>=1.13.0

# Map visualization
folium>=0.15.0
streamlit-folium>=0.15.0

# Google Drive upload (auto-upload after scraping)
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.1.0

# Machine learning / predictive model
scikit-learn>=1.3.0
numpy>=1.24.0
