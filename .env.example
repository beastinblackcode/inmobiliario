# Twitter/X API — OAuth 1.0a
# Copia este archivo como .env y rellena con tus credenciales reales
# NUNCA subas el archivo .env a git (ya está en .gitignore)

TWITTER_API_KEY=aqui_tu_consumer_key
TWITTER_API_SECRET=aqui_tu_consumer_secret
TWITTER_ACCESS_TOKEN=aqui_tu_access_token
TWITTER_ACCESS_TOKEN_SECRET=aqui_tu_access_token_secret

# Scraping cost optimization
# FETCH_MODE: 'hybrid' (direct+proxy fallback), 'direct' (free only), 'proxy' (BrightData only)
FETCH_MODE=proxy
# Seconds between direct requests (higher = safer, lower = faster)
DIRECT_REQUEST_DELAY=2.0
# Switch to proxy-only after N consecutive direct failures
DIRECT_FAIL_THRESHOLD=10

# BrightData Web Unlocker (fallback tier — $15/GB)
# BRIGHTDATA_USER=brd-customer-hl_xxxxx-zone-web_unlocker1
# BRIGHTDATA_PASS=your_password
# BRIGHTDATA_HOST=brd.superproxy.io:33335

# BrightData Residential Proxy (primary tier — $8/GB, tried first)
# If set, residential is tried before web unlocker to reduce costs.
# Credentials default to BRIGHTDATA_USER/PASS if not specified.
# BRIGHTDATA_RESIDENTIAL_HOST=brd.superproxy.io:XXXXX
# BRIGHTDATA_RESIDENTIAL_USER=brd-customer-hl_xxxxx-zone-residential_proxy1
# BRIGHTDATA_RESIDENTIAL_PASS=your_password
